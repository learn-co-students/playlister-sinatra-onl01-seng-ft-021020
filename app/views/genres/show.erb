<h1><%= @genre.name %></h1>
<h4>Songs:</h4>
<ul>
  <% @genre.songs.each do |song| %>
    <li>
      <a href="/artists/<%= song.artist.slug %>">
        <%= song.artist.name %> - 
      </a>
      <a href="/songs/<%= song.slug %>">
        <%= song.name %>
      </a>
    </li>
  <% end %>
</ul>

